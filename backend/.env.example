# Example environment configuration for the AI-Market backend.
# Copy to .env (gitignored) or export these in your shell. The app reads
# real values from the process environment; this file is documentation only.

# PostgreSQL is the only product source. DATABASE_URL is REQUIRED for product
# loading; startup fails loudly if the catalog cannot be loaded (no CSV fallback).
# libpq URI accepted directly by psycopg2. Matches docker-compose (host port 5433).
DATABASE_URL=postgresql://aimarket_user:aimarket_pass@localhost:5433/aimarket

# Optional: override the products table name (default: products).
# PRODUCTS_TABLE=products

# Optional: core ranking v2 (directness-tier ordering so direct-intent matches
# lead). Enabled by default; set to false to use the legacy diversify/head order.
# AI_MARKET_RANKING_V2=true

# Optional: primary/alternative result grouping. Labels each result with a
# match_group and exposes primary_products / alternative_products in the
# response (the products list stays primary-first and backward-compatible).
# Enabled by default; set to false to return the ungrouped response.
# AI_MARKET_GROUPED_RANKING=true
